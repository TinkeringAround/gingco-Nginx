stages:
  - name: Build Image
    steps:
      - publishImageConfig:
          dockerfilePath: ./Dockerfile
          buildContext: .
          tag: mywebsite:prelive
  - name: Deployment
    steps:
      - applyYamlConfig:
          path: ./deployment/deployment.yaml
timeout: 60
notification: {}
