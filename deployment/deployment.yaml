apiVersion: v1
kind: Service
metadata:
  name: website
spec:
  type: NodePort
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 80
      nodePort: 30000
  selector:
    app: website
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: app
spec:
  selector:
    matchLabels:
      app: app
  replicas: 1
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: app
    spec:
      imagePullSecrets:
        - name: pipeline-docker-registry
      containers:
        - name: app
          image: ${CICD_IMAGE}:prelive
          ports:
            - containerPort: 80
